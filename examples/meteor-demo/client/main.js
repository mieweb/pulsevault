// Intentionally empty. The demo UI is not a Meteor client app — the pairing
// page ("/") and the Instagram-style feed ("/library") are self-contained
// static React pages served from private/ via WebApp.rawConnectHandlers (see
// server/main.js). Meteor only requires a client mainModule to exist.
